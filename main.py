def ranking_assignment(strength, stamina, intelligence):
  # strength measurements
  if strength[0]
  if strength[1]
  if strength[2]
  # stamina measurements
  # intelligence measurements

ranks = 

class Profile:
  def __init__(self, rank, status, title):
    for rank in ranks:
      if rank == self.rank():
        
